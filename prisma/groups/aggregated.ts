import { permissions as memberPortalPermissions, groups as memberPortalGroups } from './memberPortal';

const permissions = [
    ...memberPortalPermissions,
];

const groups = [
    ...memberPortalGroups,
];

export { permissions, groups }
